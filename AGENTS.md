# Repository Working Agreement

## Mission

Implement the MATLAB/Psychtoolbox imagined-speech experiment described in `CLAUDE.md`. Read `CLAUDE.md` before changing runtime behavior. Keep the task runnable by block (0–4), deterministic, data-driven, preload-first, timing-aware, and safe around participant data and lab hardware.

## Current repository state

The launcher, config loading/validation, hardware adapters, and all five block runners are implemented (`run_imagined_speech.m`, `src/config/`, `src/core/`, `src/io/`, `src/blocks/`). All five blocks have finalized `task_data/` content (instructions, story manifests with answer keys, the Block 1 speech/imagery schedule with its audio manifest, the Block 2 image one-back sequence, and the 16-item VVIQ survey transcribed from the reference PDF) and their loaders have been smoke-tested via MATLAB `-batch` on this machine. Do not assume every asset is finalized merely because a file exists, though — always spot-check before relying on memory of this state. Block 1's and Block 2's schedule/sequence CSVs are row-count-agnostic (see below) and get swapped out during active iteration, so don't treat any specific trial count mentioned in past notes here as current — check `height(schedule)` from the loader, or the file itself, instead.

Block numbering matches GUI presentation order: 0 Instructions, 1 Speech/Imagery, 2 Image Encoding, 3 Stories & Comprehension, 4 VVIQ. Block 1 supports a configurable `presentation_mode` (`listening`/`reading`/`both`, default `both`) with independently configurable reading-only durations for words vs. phrases; Block 2's on-screen image size is configurable via `block2.image_display_fraction`. Blocks 3 and 4 support a deselect key (`config.keys.deselect`, default BackSpace, never Escape) that clears a picked-but-unsubmitted multiple-choice answer so the participant can reconsider it, emitting a paired photodiode/comment `ANSWER_DESELECT` event. Every block from 1-4 calls `src/core/save_run_snapshot.m` once per run (right after opening its CSV logs, before any timed trial) to write `block{N}_{runId}_config.mat` into the session directory: the full resolved config, MATLAB/OS/Psychtoolbox/hostname metadata, and SHA-256 hashes (via `src/core/compute_file_hash.m`) of every task-data file that determined that run's content — so which manifest/schedule/survey version produced a given dataset is always reconstructable, without duplicating that into the already-incremental per-trial CSVs. Cbmex `$TASKID` naming uses a compact per-block name (`src/core/task_block_name.m`: `Instructions`, `SpeechImagery`, `ImageEncoding`, `StoriesComprehension`, `VVIQ`) rather than a bare block number, e.g. `ImaginedSpeech_SpeechImagery_<PatientID>`.

A third sync channel, `audio_sync` (default disabled), plays short synthesized pure tones through a dedicated PsychPortAudio device — separate from any block's stimulus-audio handle — via `src/core/{build_sync_tone,init_sync_tones,play_sync_tone,finish_sync_tones}.m`. All 5 blocks play `block_start`/`block_end` (the latter via `onCleanup`, so it fires on success/abort/error alike); Block 1 adds `block1_reading_stimulus_onset` (reading-mode only), `block1_early_response`, and `block1_action_cue_onset`/`block1_action_cue_offset` (green cue appearing / disappearing as the ITI begins); Block 2 adds `block2_image_onset` and `block2_response` (its only two tone triggers, by design); Block 3 adds `block3_question_onset`; Block 4 adds `block4_question_onset`. Each tone can override the block-level default volume via `audio_sync.tones.<name>.volume` — Block 1's two new cue tones and Block 2's two tones are all set to `0.15` (vs. the `0.3` default) since they're meant to read as quieter/more understated. Confirmed PsychPortAudio works fine from this shell (no interactive-desktop dependency), including two simultaneous handles to the same default device, so all audio-sync tone playback was verified end-to-end (audibly played, including confirming the quieter tones' synthesized peak amplitude is exactly half the louder ones', and config validation exercised for missing/invalid tone specs and out-of-range per-tone volume) rather than only statically checked. (At the time this was written, `Screen('OpenWindow', ...)` was believed to hang from this shell, making this a contrast; that belief turned out to be wrong — see below.)

These changes were verified via MATLAB static code check (`checkcode`), config-validation tests (valid and invalid values), a full loader regression pass, and end-to-end snapshot-save/read and tone-playback tests. **Correction to an earlier note in this file**: `Screen('OpenWindow', ...)` does *not* actually hang from this shell — that was re-tested from scratch and disproven (see below). What genuinely cannot be done from here is simulating a keypress: this machine's PsychHID keyboard capture ignores software-injected key events entirely (confirmed in isolation, see below), so any change whose correctness depends on a simulated Enter/Escape/pause/response still needs a human at the console — visual/timing changes that don't depend on a keypress can now get a real live check from here. No `tests/` directory exists yet; verification so far has been manual data-loader smoke tests plus static checks, not an automated suite.

**Important discovery**: unlike `Screen`, `cbmex` genuinely works from this non-interactive shell when `config.cbmex.enabled` is true — it connects to this machine's real NSP1/NSP2 hardware and can append real entries to the live `+CurrentPatientLog`. Be careful with any script that calls `start_task_lifecycle`/`finish_task_lifecycle` against the live config: an uncaught bug in a *test* script (e.g. forgetting to set `config.session.patient_id`) can trigger a real `$TASKSTART`/`$TASKID`/`$TASKERR` cycle and increment the real patient log, not a harmless no-op — this happened twice while testing. Always use an obviously-fake patient ID and get explicit confirmation before running lifecycle code against the live config from this environment. The same caution applies to `config.matcbsdk` — its `Session` objects are a second, independent path to the same real Blackrock hardware and `+CurrentPatientLog`; PsychPortAudio-style "safe to test freely" does not apply here.

**Also learned the hard way**: creating `Session` objects across several separate MATLAB `-batch` invocations in quick succession (each its own process, testing `start_task_lifecycle`'s matcbsdk path against real reachable NSP hardware) crashed MATLAB outright with an access violation inside `cbsdk.dll` (`cbsdk_session_unregister_callback`). This matches a documented pre-existing issue in the CereLink SDK itself (`Matlab/cerelink/README.md`'s "Known issue: don't rapidly recreate sessions" — a teardown race in `cbsdk_session_destroy`, reproduced by CereLink's own authors in a standalone C++ program with no MATLAB involved), not a bug introduced here. Practical upshot: don't loop or rapid-fire `Session(...)`/`start_task_lifecycle` matcbsdk tests back-to-back from this shell when real hardware is reachable — space them out, and prefer testing the surrounding logic (config validation, the `usePatientLog` branch, etc.) without touching `Session` at all when the hardware-touching part has already been verified once.

Blocks 1-3 gained pause/resume: `keys.pause` (default `p`, validated distinct from `keys.abort`) triggers a shared `src/core/wait_for_pause_resume.m` overlay from any pausable wait loop, extending that phase's deadline (or, for audio, stopping/resuming PsychPortAudio from its exact position) by the elapsed pause duration on return. Per the user's explicit scope, Block 3 only wires this into `waitForStory` (story-audio playback) — not the comprehension-question UI, which has no time pressure to begin with. **Found and fixed a real bug while testing this in isolation** (not just reasoned about): resuming playback with `PsychPortAudio('Start', ..., waitForStart=0)` can return before `GetStatus().Active` actually flips true, so the very next poll in the wait loop sees `Active=0` and exits immediately as if the audio had already finished — reproduced with a synthetic 2-second tone (stop at ~0.6s, resume after a simulated pause, wait loop exited in ~0.001s instead of the expected ~1.35s remaining). Fixed by using `waitForStart=1` (blocking) on every resume `Start` call, then reproduced the same test and confirmed the full remaining duration now plays out correctly (~1.335s measured for a ~1.35s remainder). This is exactly the kind of thing that looks correct on code review alone but only surfaces by actually running it.

**A second, more serious bug shipped in that same pause work and was only caught by the user testing live on real hardware, not by anything above**: Block 3's `waitForStory` (and, ported from the identical pattern, Block 1's `waitAudio1`) drove their entire polling while-loop off `while PsychPortAudio('GetStatus', h).Active` — meaning the loop only ever checked for Escape/pause keys *as long as the audio device reported itself active*. On the user's real hardware this left Block 3 completely unresponsive to Escape (not just pause) for the whole multi-minute story — a task that could not be aborted, found via live testing after the fact. Block 1's word/phrase clips are only ~1-3s, so the same latent bug there never actually got exercised in testing, creating a false impression that only Block 3 was affected. Root-caused as an audio-status-driven loop condition being fundamentally the wrong mechanism for keyboard-responsiveness (regardless of the exact reason `GetStatus` polling can stall key detection on some systems) and fixed by converting both loops to be deadline-driven from the analytically-known clip/story duration (`size(samples,2)/sample_rate`), exactly like every other already-reliable pausable phase (silence/reveal/ITI/image-on-off) — `GetStatus()` is now only ever read transiently, to capture `PositionSecs` at the moment a pause is actually requested, never as the while-loop's own stopping condition. Lesson for future sync-related work here: an audio-active-flag-driven loop condition is a shape of bug that is very easy to miss in isolated testing (short clips mask it) and only shows up against real long-duration playback — prefer deadline arithmetic over device-status polling for loop control whenever the duration is knowable in advance.

Added mid-block resume options: `config.block1.start_trial` / `config.block2.start_trial` (positive integer, default 1; validated against the actual schedule row count at runtime, not just config-load time, since the loader isn't available yet when `load_task_config` runs) and `config.block3.start_position` (`"story_1"`/`"comprehension_1"`/`"story_2"`/`"comprehension_2"`, default `"story_1"`). Block 3's implementation wraps only the ready-screen+audio-playback portion of the story loop in a conditional, skipped exactly once (for the starting story only, never a later one in the same run) when starting at a `"comprehension_*"` position; skipped trials/stories are genuinely never run or logged, not fast-forwarded through.

`wait_for_pause_resume.m` now also takes `toneState` and plays new `pause_on`/`pause_resume` audio_sync tones (both `volume: 0.1`, the quietest tier in the config, since they're pure timekeeping markers) alongside the photodiode pulse it already did — all three sync channels (photodiode, Cbmex/matcbsdk comment via the caller's own emit function, audio tone) now fire consistently on every pause/resume, not just the visual one.

**Found and removed a genuine hardcoded-trial-count bug**: `load_block1_content.m` rejected any `AAC_trial_schedule.csv` that wasn't exactly 540 rows (`height(schedule)~=540`, literal), which surfaced as a confusing "not expected number of trials"-style error the moment the user tried swapping in a differently-sized schedule for real content iteration. `load_block2_content.m` never had this problem — it was already fully row-count-agnostic, deriving everything from `height(schedule)`. Fixed Block 1's loader to do the same: `trialCount=height(schedule)` computed dynamically, then validated for gap-free/duplicate-free ordering `1..trialCount` rather than a fixed `1..540`. Verified against the user's actual current (already-swapped-in) content — which loaded successfully at 120 schedule rows and 115 image-sequence rows, neither of which would have passed the old Block 1 check — and against a synthetic 10-row schedule built fresh for this test; also confirmed a schedule with duplicate/non-contiguous `trial_number` values is still correctly rejected, so the fix loosens the row-count assumption without loosening the structural-integrity checks (unique/ordered trial numbers, valid trial types/durations, every stimulus resolving to an audio file that actually decodes).

**The deadline-vs-audio-status rewrite above did not actually fix Block 3's unresponsiveness** — the user retested on real hardware and Escape/pause were still completely undetectable for the whole story. This matters: it means the bug's root cause was never "which condition the while-loop checks" at all (that fix was still worth keeping, since a non-blocking resume `Start` genuinely can misreport `Active`, but it wasn't *the* bug making Escape unresponsive). Re-examined `waitForStory`'s logic line by line and found nothing wrong with it — `while GetSecs < deadline` checking `KbQueueCheck` every ~10ms is structurally identical to every other already-reliable pausable wait in this codebase. That points at something *outside* the loop's own logic: every block calls `Priority(MaxPriority(window))` once at window-open and never lowers it until block-end cleanup, to protect `Screen('Flip', ...)` timing from OS preemption — but during the story-audio wait specifically, there is no Flip happening at all for however long the story runs, so elevated priority is protecting nothing there while potentially starving the keyboard listener thread of scheduling time over a sustained multi-minute busy-poll. This is consistent with both observations: Block 1's word/phrase clips (~1-3s) never ran long enough at elevated priority to exhibit it, and the deadline-based rewrite alone couldn't have fixed a problem that was never in the loop's stopping condition. Fixed by having `waitForStory` (Block 3) and, preventatively, `waitAudio1` (Block 1) drop `Priority` to normal for their own duration via `oldPriority=Priority(0)` + `onCleanup(@()Priority(oldPriority))`, restoring on every exit path.

**Follow-up, same session: the user pushed back hard on "unverified," correctly** — pointed out MATLAB/Psychtoolbox is genuinely usable from here and asked for real verification, not another guess. That prompted re-testing the "`Screen('OpenWindow', ...)` hangs from this shell" claim above from scratch (fine-grained `fprintf` progress markers around every step): **it was wrong**. A window opened, rendered, flipped, and closed in seconds. This shell runs in Windows session 1 (`console`), the same session the logged-in user (`lizzi`) is actually active in — there was never a session-isolation reason for a hang; whatever caused the original hang was something else (most likely an old test script blocked on a real keypress it never provided, misread as "Screen itself hung"). That unblocked running `run_block_3` for real: a real session directory, real story audio (confirmed durations 205s and 345s), `test_auto_advance_seconds` to skip the ready-screen wait, and `test_max_story_seconds=100` as a safety cap. The live run confirmed the window, real audio playback, and the deadline-based timing all work correctly (the wait ended exactly at the 100s cap, not early/late), and the block completes end-to-end without error.

**It could not confirm whether Escape/pause are actually now detected**, because of a deeper, unrelated wall: simulating a keypress requires software input injection, and this machine's PsychHID keyboard capture does not respond to it *at all*. Proven with a minimal isolation test with zero shared complexity from the story-audio scenario — a bare `Screen`+`KbQueueCreate`/`KbQueueCheck` loop, no audio, no `Priority` changes, a 90-second detection window, `SetForegroundWindow` on the exact target window confirmed to return `True` by matching PID (`feature('getpid')` from MATLAB, cross-checked with `Get-Process -Id`) — an injected `p` via Win32 `keybd_event` was never detected, not once, across multiple repeated attempts with generous timing margin. This is very likely intentional (or at least a reasonable side effect) for a research tool that needs to trust its recorded responses come from a real participant rather than automation, but the practical upshot is absolute: **no `keybd_event`/`SendInput`-style synthetic input can be used to verify any keypress-driven behavior in this codebase from automation, ever, on this machine** — not specific to this bug. Real physical key presses from a person at the console remain the only way to confirm Escape/pause responsiveness (or anything else keypress-gated) here. If Block 3 is still unresponsive after this fix on a real console test, the priority-starvation hypothesis is disproven and something more machine/driver-specific needs investigating with the user present to press real keys — e.g. instrumenting with periodic `fprintf` timestamps to see whether the loop is even executing during the unresponsive period.

All of the above verified: static checks clean, full config/loader regression passes, `start_trial`/`start_position` validation (bad values rejected, all 4 position enum branches resolve correctly) tested, and `pause_on`/`pause_resume` tones confirmed to synthesize at the correct quiet amplitude and play audibly.

`config.cbmex.use_patient_log` / `config.matcbsdk.use_patient_log` (both required booleans, default `true`) make the `+CurrentPatientLog` EMU-number allocation optional per backend — added after a real run on this rig hit `getNextLogEntry` erroring with "Output argument ... not assigned a value" (that rig's patient log isn't set up). With the flag `false`, `start_task_lifecycle` skips `getNextLogEntry`/`setNextLogEntry` entirely and every `$TASKSTART`/`$TASKID`/`$TASKSTOP`/`$TASKKILL`/`$TASKERR` comment uses the patient ID directly (stored as `lifecycle.id_label`) instead of an `EMU-####` label; `finish_task_lifecycle` was changed to read `lifecycle.id_label` rather than formatting `EMU-%04d` from `lifecycle.emu_id` itself, so it's agnostic to which mode produced the label. The shipped config now has `matcbsdk.use_patient_log: false` (this rig) and `cbmex.use_patient_log: true` (default, unchanged). Verified: config validation (missing field, non-boolean value) both rejected correctly; the no-hardware-reachable path (`NoOnlineNSP`) still works. The `usePatientLog: false` comment-formatting path itself has NOT been verified against real hardware from this session, for the crash-risk reason above — that's for the next real run on the rig to confirm.

`config.matcbsdk` is now a second comment backend alongside `config.cbmex` (`load_task_config` errors if both are enabled at once), for machines — like this one — where Blackrock Central/`cbmex` isn't installed but the compiled `matcbsdk` MEX wrapper is (`C:\Users\lizzi\Documents\CereLink\Matlab\cerelink`; `Session.m` classdef, `s.send_comment(text, rgba, charset)`, `s.close()`). On this machine, `exist('cbmex','file')` is `0` (not installed) while `exist('Session','file')`/`exist('matcbsdk','file')` both resolve, meaning `cbmex.enabled: true` (the current shipped default) would actually fail here with `ImaginedSpeech:MissingLifecycleDependency` if a block were run — cbmex was presumably verified on a different machine/session that had Central installed. `start_task_lifecycle`/`finish_task_lifecycle`/`send_task_event_comment` all branch on `config.comment_lifecycle.backend` (`"cbmex"` or `"matcbsdk"`) rather than duplicating logic; `config.comment_lifecycle` replaces the old `config.cbmex.lifecycle` slot. Tested from this shell with a fake patient ID (`'TESTPT'`): `Session('NSP')` creation itself works fine and fails fast (~2s, no hang) with a clean `Device connection error` when no NSP is reachable via that path, and the full `start_task_lifecycle` matcbsdk branch correctly surfaces `ImaginedSpeech:NoOnlineNSP` in that case — without ever reaching the patient-log-writing code, so this was safe. Config validation (both-enabled rejected, empty `device_types` rejected, valid matcbsdk-only config accepted) was also exercised.

A prior end-of-block crash (MATLAB dying outright instead of sending `$TASKSTOP`) was traced to Block 2 (Image Encoding) closing its Psychtoolbox textures twice — once explicitly at the end of the trial loop, again via the `onCleanup` handler that already runs on every exit path. Double-closing a texture handle is a double-free that can crash the process rather than raising a catchable MATLAB error, which is why it bypassed `run_imagined_speech.m`'s try/catch entirely. Fixed by removing the redundant explicit close; all blocks' screen-cleanup calls were also hardened with individual try/catch. Verified end-to-end against live NSP hardware (see above) that normal completion, Escape, and error paths now correctly send `$TASKSTOP`/`$TASKKILL`/`$TASKERR` and close connections without crashing.

Separately, every photodiode flash was widened from exactly one refresh frame to `photodiode.flash_frames` frames (default 3) — a single-frame pulse was the leading suspect for flashes intermittently not registering on the physical photodiode. Blocks 3 and 4 gained a `HIGHLIGHT_MOVE` flash/comment on every Up/Down navigation press. Trying `display.skip_sync_tests: false` was a further diagnostic attempt but backfired hard: on this machine it makes Psychtoolbox hard-abort every block with a `SYNCHRONIZATION FAILURE` against the Windows DWM desktop compositor (which cannot be fully disabled on Windows 10/11) — a well-documented PTB-on-Windows false positive, not evidence of an actual presentation-timing problem — so it's back to `true`. Keep `skip_sync_tests: true` on this machine unless someone specifically wants to fight the DWM/PTB interaction; `flash_frames` is the fix that actually matters for the photodiode symptom and doesn't depend on it.

Block 2 kept proccing inconsistently even after `flash_frames`, for a reason specific to it: it's the only block where a participant response can land at an arbitrary time inside an otherwise fixed-schedule phase, so its `RESPONSE` flash could end up close enough to a scheduled transition's own flash to blend together on the photodiode. A briefly-added `IMAGE_OFFSET` flash on the image-off transition made this worse (it collided with `RESPONSE` flashes from the on-phase); it was removed, that transition is now flash/comment-silent, and the *following* trial's `IMAGE_ONSET` is delayed until any pending `RESPONSE` flash has fully completed (see `respFlashEnd` handling in `run_block_2.m`) so a late off-phase response can't collide with it either. General lesson for any future sync work here: two flashes close in time is the failure mode to watch for, not missing code — one comes from a fixed schedule, the other from an unpredictable participant action, and they need either a guaranteed minimum gap or one of them dropped.

Block 2 was still reported as intermittently dropping flashes after that fix, with `$TASKSTART`/`$TASKID`/comments during the block otherwise sending fine. Found that none of the five blocks ever actually raise MATLAB's OS scheduling priority, despite every block's cleanup resetting `Priority(0)` — a leftover expectation from priority having been raised that was apparently never implemented. Added `Priority(MaxPriority(window))` right after each block opens its window. `send_task_event_comment.m` is architecturally decoupled from Screen/the compositor — a `cbmex('comment', ...)` failure is uncaught and propagates to a visible `$TASKERR`/crash, so there's no path for a comment to silently drop while the block keeps running; a trial with a missed visual flash should still have a comment logged on the NSP side, which is worth cross-checking empirically against the physical photodiode channel if the intermittency persists. None of this has been visually verified against a live Psychtoolbox window or physical photodiode from this environment — needs a console check.

## Non-negotiable constraints

- Never hardcode participant-facing stimuli, instructions, questions, choices, answer keys, image order, story order, or survey content in MATLAB.
- Put runtime content in validated manifests/schedules beneath `task_data/`. Code may contain only generic UI labels and stable event identifiers.
- Load and validate every asset required by the chosen block before the block begins. Do no file/network/document parsing or media decoding during timed trials.
- Treat timing as scheduled Psychtoolbox activity: anchor durations to measured flip/audio timestamps, not chained wall-clock pauses.
- Photodiode, Cbmex, and matcbsdk must be independently toggleable in config and routed through a shared canonical event layer. Cbmex and matcbsdk are alternate NSP comment backends, not additive — enabling both is a config error, never a "use both" state.
- The master launcher, not individual blocks, owns Cbmex/matcbsdk connections and lifecycle comments. With comments enabled it must mirror the lab `TaskComment` contract: TaskStart/TaskID plus one patient-log increment, then exactly one of TaskStop, TaskKill, or TaskErr using the same EMU number.
- When a configured synchronization backend fails to initialize, stop before collecting block data. Disabled backends must use no-op adapters without requiring lab hardware.
- Always provide a single emergency-abort path and guaranteed cleanup for `Screen`, PsychPortAudio, cursor visibility, keyboard restrictions/queues, priority, and Cbmex resources. Use `onCleanup` and top-level `try/catch` protection.
- Save incrementally to the launcher-created, time-indexed patient session path. Blocks 1, 3, and 4 must write behavioral data there. Do not overwrite or delete participant results.
- Do not read, print, copy, or commit `task_data/speech/eleven_labs_api.txt`. Do not commit the future ElevenLabs generation script or generated speech files unless the user explicitly changes this policy.

## Preferred architecture

Keep the public entry point small. A suitable decomposition is:

```text
run_imagined_speech.m       launcher, participant/session input, block GUI
config/                     versioned runtime configuration
src/config/                 loading and validation
src/core/                   PTB setup, cleanup, clocks, logging, events
src/io/                     block-specific manifests and preloaders
src/sync/                   photodiode, Cbmex, and no-op adapters
src/blocks/                 one runner per block
tests/                      unit tests and short fixture-based smoke tests
task_data/                  external runtime content and media
```

This layout is a guide, not permission to create empty abstractions. Prefer small MATLAB functions with explicit inputs/outputs over scripts, globals, persistent hidden state, or a single monolithic task file.

Represent loaded state explicitly, for example with `config`, `session`, `devices`, `buffers`, and `logger` structs. Block runners should not reinitialize shared hardware unnecessarily and should not access secrets.

## Data contracts

Define and validate schemas before implementing each block. Prefer JSON for nested manifests/questions and CSV for trial schedules. Store relative asset paths relative to the manifest (or define one documented project-root convention); reject traversal outside approved task-data roots.

At minimum:

- Block 0 manifest: ordered page IDs and page content (or referenced UTF-8 text files).
- Block 1 manifest: ordered story IDs, audio paths, ordered questions, choices, and optional correct-choice IDs.
- Block 2 schedule: `trial_number`, `word`, `trial_type`, `silent_period_duration`, `inter_trial_interval_duration`, plus an explicit one-row-per-unique-stimulus audio manifest mapping stable stimulus ID/text to one shared file.
- Block 3 manifest/schedule: trial number, image ID/path, and intended one-back target status (or enough explicit sequence information to validate it).
- Block 4 manifest: survey/version metadata, ordered item IDs/text, response labels/values, and documented scoring metadata.

Use stable IDs in logs; display text is not a safe primary key. Validate schedules for row count/order, allowed trial types, finite nonnegative durations, referenced files, duplicate IDs, expected repetitions, and consistency between declared and computed targets.

## Timing and input rules

- Use `Screen('Flip', window, when)` and returned timestamps for visual timing.
- Use PsychPortAudio for low-latency playback and capture actual start timing when available.
- Use keyboard queues for responses when timing matters; debounce launcher/survey navigation.
- Normalize key names with `KbName('UnifyKeyNames')`.
- Convert desired durations to refresh-aware deadlines and log missed flips.
- Create textures and decoded audio buffers before the ready screen, then reuse and explicitly release them.
- The experiment display remains black unless task data/config specifies otherwise. The green action cue and photodiode patch must be drawn before the same flip used to timestamp their onset.

## Event and logging rules

Maintain a central event dictionary with stable, documented names/codes. A single event emission call should:

1. timestamp the behavioral event;
2. append it to the recoverable log;
3. issue the configured Cbmex comment;
4. coordinate the configured photodiode state for flip-bound visual events.

Do not claim sub-frame simultaneity between software comments, audio, and display. Log the distinct timestamps and measured relationships. Include block/trial/stimulus/action identifiers in events without exceeding acquisition-system comment limits.

## Development sequence

Follow the implementation stages in `CLAUDE.md`. Before building a block, first add its schema/validator and a minimal fixture. Before running a full 540-trial block, prove the same path with a short schedule. Hardware-free development must work in debug mode with synchronization disabled; hardware integration is a separate verification step, not a reason to bypass validation.

Do not invent missing scientific content or acquisition codes. Add an explicit placeholder/schema and surface a clear preflight error describing what is missing. Ask the user when a decision changes experimental meaning.

## Verification expectations

For every material change:

- run MATLAB unit tests when MATLAB is available;
- otherwise perform static/data validation that is possible locally and state what could not be run;
- test success, abort, missing asset, malformed schedule/config, disabled hardware, and enabled-but-unavailable hardware paths as relevant;
- keep test fixtures small and synthetic; do not duplicate participant stimuli into source code;
- inspect Git status and avoid modifying unrelated/user-owned files.

Timing acceptance must ultimately be checked on the actual acquisition computer with its display, audio interface, photodiode, and Blackrock/Cerebus setup. Desktop smoke tests cannot establish neural synchronization accuracy.

## Secrets and generated files

The ElevenLabs API key and local generator are preparation-only artifacts. Ensure ignore rules cover the exact secret/script/generated-output patterns before generator work begins. Prefer a manifest/checksum report that verifies complete stimulus coverage without exposing credentials. Never use live generation as a fallback for a missing experiment asset.

## Documentation discipline

Update `CLAUDE.md` when the experimental contract changes and update this file when repository-wide implementation rules change. Document schema versions and event-code changes alongside code. Keep unresolved experimental choices visible; do not quietly convert assumptions into protocol.
